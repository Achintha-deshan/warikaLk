import type { ReactNode } from 'react'

export default function ConfirmDialog({ title, body, confirmLabel = 'Confirm', onConfirm, onCancel, loading = false, confirmDisabled = false, children }: { title: string; body: string; confirmLabel?: string; onConfirm: () => void; onCancel: () => void; loading?: boolean; confirmDisabled?: boolean; children?: ReactNode }) {
  return <div className="dialog-backdrop" role="presentation"><section className="dialog" role="alertdialog" aria-modal="true" aria-labelledby="dialog-title"><p className="eyebrow">Please confirm</p><h2 id="dialog-title">{title}</h2><p>{body}</p>{children}<div className="dialog-actions"><button className="secondary-button" type="button" onClick={onCancel} disabled={loading}>Cancel</button><button className="primary-button danger-button" type="button" onClick={onConfirm} disabled={loading || confirmDisabled}>{loading ? 'Working...' : confirmLabel}</button></div></section></div>
}